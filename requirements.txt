# --- Core engine (required) ---
reportlab==4.2.5          # vector PDF generation (calendar pages)
pillow==11.0.0            # raster compositing (mockups, procedural art)
pypdfium2==4.30.0         # PDF -> PNG rasterizing for mockup previews
python-dotenv==1.0.1      # .env loading

# --- Frontend (dashboard + accounts) ---
streamlit==1.60.0

# --- AI images (optional: falls back to procedural art when absent) ---
openai==1.59.6

# --- Etsy integration (encrypts OAuth tokens at rest) ---
cryptography==50.0.0

# --- HTTP (used by the optional Canva Connect client) ---
requests==2.32.3
